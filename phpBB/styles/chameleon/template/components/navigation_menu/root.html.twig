<nav aria-label="Main" data-orientation="horizontal" dir="ltr" class="relative z-10 max-w-max flex-1 items-center justify-center {{ className|default('') }}">
	<div class="relative">
		{{ block('content') }}
	</div>
	<div class="absolute left-0 top-full flex justify-center"></div>
</nav>

